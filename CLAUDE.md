# 작업 규칙

- 이 폴더의 작업 기준 문서는 [서비스-기획서.md](서비스-기획서.md) 다. 기능/화면/데이터 규칙 등은 이 문서를 기준으로 판단하고, 문서에 없는 내용은 임의로 추가하지 않는다.
- 결과물은 `index.html` 파일 하나로 만든다. 설치나 빌드 과정 없이 브라우저에서 파일을 열면 바로 동작해야 한다. 단, 여러 사용자가 데이터를 공유해야 해서 Supabase(Postgres) 백엔드를 사용한다 — 인터넷 연결과 Supabase 프로젝트가 필요하며, Supabase 프로젝트 URL과 anon key는 index.html 안에 포함되어 있다(접속한 누구나 데이터를 읽고 쓸 수 있는 구조, 민감 정보 저장 금지).
- 화면 문구·설명은 모두 한국어로 작성한다.
- 디자인은 깊은 네이비색(#0F172A) 배경에 은은한 푸른빛 그라데이션을 깔고, 카드(패널)는 반투명하고 뒤 배경이 흐리게 비치는 글래스모피즘(backdrop-blur) 스타일로 한다. 포인트 색은 하늘색 계열(#35D7FF) 1개를 사용한다. 모바일 화면 폭에서도 레이아웃이 깨지지 않게 반응형으로 만든다.
- 화면 구조나 기능을 크게 바꾸는 작업 전에는, 무엇을 바꿀지 한 줄로 먼저 알리고 진행한다. 작은 수정(오타, 스타일 미세조정 등)은 바로 진행해도 된다.
