# 활성탄 통합관리 대시보드 - 서비스 기획서

## 무엇을 만드나 (한줄요약)
정수장(고도처리)·활성탄 재생시설·활성탄 비축창고의 데이터를 연계해, 활성탄 교체 시점을 예측해 사전보고서로 보여주고, 재생·창고 공급 우선순위를 시뮬레이션으로 제안하며, 관련 기준을 답해주는 챗봇을 제공하는 웹 대시보드.

## 누가 쓰나
- 정수장(고도처리) 운영자 / 수질관리 담당자 — 교체 시점 예측·사전보고서 확인
- 활성탄 재생시설 담당자 — 재생 우선순위, 재생 스케줄 확인
- 비축창고 담당자 — 재고 수준, 공급 우선순위 확인
- (낙동강유역 여러 정수장을 총괄하는) 관리자 — 유역 전체 현황 모니터링

## 핵심 기능 (화면에서 할 수 있는 것)

### 1. 활성탄 교체 시점 예측
- 정수장별 활성탄 상태를 계절, 수질 이벤트, 요오드값, DOC, DBPs, geosmin&MIB, 운영기간 등의 지표로 종합 평가
- 정수장을 선택하면 "교체 예상 시점"과 "예측 근거"를 확인
- 지표별 추이를 그래프로 확인 (시간에 따른 요오드값, DOC 등 변화)
- 위 내용을 담은 "사전보고서"를 화면에서 생성하고 파일(PDF 등)로 다운로드
- 교체 임박/수질 이상 등 특이사항은 목록 상단에 경고 표시

### 2. 활성탄 재생 및 비축창고 공급 최적화
- 재생시설의 처리 가능 용량, 비축창고의 현재 재고 수준을 한 화면에서 확인
- "지금 어느 정수장 활성탄을 먼저 재생해야 하는지", "어느 창고에서 어디로 공급해야 하는지" 우선순위 리스트로 제시
- 재고 수준·수요 변화를 가정해 시뮬레이션 실행 → 결과(재고 부족 예상 시점, 권장 조치)를 확인
- 시뮬레이션 조건(가정치)을 사용자가 조정해서 재실행

### 3. 활성탄 챗봇
- 활성탄 재생 기준, 교체 판단 기준 등에 대해 자연어로 질문하면 답변
- 답변 시 참고한 기준(자료 출처/근거)을 함께 표시
- 대시보드에서 보고 있는 특정 정수장/데이터를 언급하며 질문 가능 (예: "A정수장은 왜 교체 임박으로 떴어?")

## 화면 구성 (어떤 영역, 버튼 등이 있나)

### 공통 레이아웃
- 좌측 사이드바: 메뉴 이동 (전체 현황 / 정수장별 예측 / 재생·공급 최적화 / 챗봇)
- 상단: 유역 전체 요약 카드 (교체 임박 정수장 수, 재고 부족 위험 창고 수 등)

### 화면 A. 전체 현황 (홈)
- 정수장 목록 테이블 (상태 뱃지: 정상 / 주의 / 교체 임박)
- 재생시설·창고 재고 요약 카드
- "보고서 보기", "시뮬레이션으로 이동" 버튼

### 화면 B. 정수장별 예측 상세
- 정수장 선택 드롭다운
- 지표별 그래프 영역 (요오드값 / DOC / DBPs / geosmin&MIB / 운영기간 추이)
- 예측 결과 패널 (예상 교체 시점, 판단 근거 요약)
- [사전보고서 생성] 버튼 → 보고서 미리보기 + [다운로드] 버튼

### 화면 C. 재생·공급 최적화
- 재생시설 가동 현황 / 창고별 재고 현황 (막대그래프 또는 게이지)
- 우선순위 리스트 (정수장 → 재생 우선순위, 창고 → 공급 우선순위)
- [시뮬레이션 조건 설정] 패널 (가정 재고, 가정 수요 등 입력)
- [시뮬레이션 실행] 버튼 → 결과 그래프 + 권장 조치 텍스트

### 화면 D. 챗봇
- 채팅창 (질문 입력창 + 대화 이력)
- 답변 하단에 "참고 기준" 표시 영역
- 자주 묻는 질문 버튼 (예: "재생 기준이 뭐야?", "교체 시점은 어떻게 판단해?")

## 데이터·규칙

### 다뤄야 하는 데이터 항목
- 정수장(고도처리): 계절, 수질 이벤트 기록, 요오드값, DOC, DBPs, geosmin&MIB, 활성탄 운영기간
- 활성탄 재생시설: 처리 용량, 가동 현황, 재생 이력 (※ 구축 중 → 데이터 없음, 가상데이터로 대체)
- 활성탄 비축창고: 재고 수준, 입출고 이력 (※ 구축 중 → 데이터 없음, 가상데이터로 대체)

### 데이터 관련 규칙
- 정수장 데이터는 현재 수기 입력 → 이번 버전에서는 수기 입력 화면(또는 엑셀 업로드) + 저장된 값을 기준으로 동작
- 재생시설·비축창고 데이터는 실데이터가 없으므로, 정수장 데이터 패턴을 참고해 그럴듯한 가상데이터를 생성해 사용
- 재생기준 출처: K-water연구원, 「입상활성탄 재생기준 수립과 평가 기술」(2012) — 낙동강수계 기준만 도입 (한강수계 기준은 이번 범위 제외)

### 활성탄 재생 판단 기준 (낙동강수계)
아래 항목 중 하나라도 초과하면 "재생 권고" 상태로 판단
- 운영기간 또는 Bed Volume: 2~3년 경과 또는 BV 50,000 도달
- 요오드가(활성탄 자체 값): 600 mg/g 이하로 저하
- DOC (GAC 유출수 농도): 10 mg/L 이상
- DBPs (GAC 유출수 농도): 수질기준의 80% 이상
- Geosmin&MIB (GAC 유출수 농도): 10 ng/L 이상
- 기타 신규 오염물질/수질사고: 이번 버전에서는 미반영 (추후 수질사고 발생 시 별도 기준 추가 예정, 원자료에도 "향후 제정"으로만 명시됨)

### 재생탄 품질 기준 (재생 완료 판정용, 낙동강수계 적용치)
- 요오드가 850 mg/g 이상 (환경부고시 제2008-69호)
- 재생수율 80% 이내 (손실율 20% 기준)
- 입도분포: 신탄 대비 상한값 초과 15% 이하, 하한값 미만 5% 이하
- 유효경·균등계수·마모지수·경도: ANSI/AWWA B604, KSM1802 기준 준용 (한강·낙동강 공통 항목)

## 이번에 안 만드는 것 (오늘 범위 밖)
- 실제 정수장/재생시설/창고 시스템(SCADA 등)과의 실시간 연동
- 사용자 로그인/권한 관리 (관리자·현장직원 구분 등)
- 모바일 전용 화면
- 낙동강수계 외 타 수계(한강 등) 재생기준 반영 — 이번 테스트는 낙동강 정수장 대상, 낙동강 기준만 적용
- 보고서의 정식 양식(회사 서식) 적용 — 우선은 핵심 정보 위주의 기본 양식으로 생성
- 알림 발송 기능 (문자/메일 등으로 교체 임박 알림 전송)
